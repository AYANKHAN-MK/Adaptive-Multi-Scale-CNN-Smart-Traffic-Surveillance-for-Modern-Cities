<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Smart Traffic Surveillance - Adaptive Multi-Scale CNN</title>
    <!-- Load Tailwind CSS via CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Use Inter font family -->
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #0d1117; /* Dark background */
            color: #c9d1d9; /* Light text */
        }
        .container-padding {
            padding-left: 1rem;
            padding-right: 1rem;
        }
        @media (min-width: 640px) {
            .container-padding {
                padding-left: 2rem;
                padding-right: 2rem;
            }
        }
    </style>
</head>
<body class="min-h-screen">

    <!-- Header & Navigation -->
    <header class="sticky top-0 z-10 bg-gray-900/90 backdrop-blur-sm shadow-lg">
        <div class="max-w-7xl mx-auto container-padding flex justify-between items-center h-16">
            <h1 class="text-xl font-bold text-teal-400">AMS-Traffic AI</h1>
            <nav class="hidden sm:flex space-x-6 text-sm font-medium">
                <a href="#introduction" class="hover:text-teal-400 transition duration-150">Overview</a>
                <a href="#features" class="hover:text-teal-400 transition duration-150">Features</a>
                <a href="#dashboard" class="hover:text-teal-400 transition duration-150">Demo Status</a>
                <a href="#contact" class="hover:text-teal-400 transition duration-150">Contact</a>
            </nav>
            <button id="mobile-menu-button" class="sm:hidden p-2 rounded-lg hover:bg-gray-700 transition duration-150">
                <svg class="w-6 h-6 text-teal-400" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path></svg>
            </button>
        </div>
    </header>

    <!-- Mobile Menu (Hidden by default) -->
    <div id="mobile-menu" class="hidden sm:hidden bg-gray-900 border-t border-gray-700">
        <nav class="flex flex-col p-4 space-y-2">
            <a href="#introduction" class="block p-2 rounded-md hover:bg-gray-700 transition duration-150">Overview</a>
            <a href="#features" class="block p-2 rounded-md hover:bg-gray-700 transition duration-150">Features</a>
            <a href="#dashboard" class="block p-2 rounded-md hover:bg-gray-700 transition duration-150">Demo Status</a>
            <a href="#contact" class="block p-2 rounded-md hover:bg-gray-700 transition duration-150">Contact</a>
        </nav>
    </div>

    <main class="max-w-7xl mx-auto py-12 md:py-20 container-padding">

        <!-- Hero Section -->
        <section id="introduction" class="text-center mb-20">
            <h2 class="text-4xl sm:text-6xl font-extrabold mb-4 leading-tight text-white">
                Adaptive Multi-Scale CNN
            </h2>
            <p class="text-2xl sm:text-3xl font-light text-teal-400 mb-8">
                Smart Traffic Surveillance for Modern Cities
            </p>
            <p class="max-w-3xl mx-auto text-gray-400 text-lg mb-10">
                The rapid growth of urbanization demands efficient traffic management. Our system automates congestion detection and anomaly identification using a deep learning-based computer vision architecture that adapts to any camera angle or resolution.
            </p>
            <a href="#features" class="inline-block px-8 py-3 text-lg font-semibold bg-teal-600 hover:bg-teal-500 rounded-full transition duration-300 transform hover:scale-[1.03] shadow-lg">
                Explore Core Technology &rarr;
            </a>
        </section>

        <!-- Feature Cards Section -->
        <section id="features" class="mb-20">
            <h3 class="text-3xl font-bold text-center mb-12 text-white">Key System Capabilities</h3>

            <div class="grid md:grid-cols-3 gap-8">
                <!-- Card 1: Multi-Scale CNN -->
                <div class="bg-gray-800 p-6 rounded-xl shadow-2xl border border-gray-700 transition duration-300 hover:border-teal-500 hover:shadow-teal-900/50">
                    <div class="text-teal-400 mb-4">
                        <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.75 17L9 20l-1 1h8l-1-1v-3.25m-7.25 0V7.5M16.5 17V7.5M12 21V3M7 7.5h10a2 2 0 012 2v10a2 2 0 01-2 2H7a2 2 0 01-2-2V9.5a2 2 0 012-2z"></path></svg>
                    </div>
                    <h4 class="text-xl font-semibold mb-2 text-white">Adaptive Multi-Scale CNN</h4>
                    <p class="text-gray-400">Improves detection accuracy for vehicles of varying sizes and perspectives by analyzing traffic footage across multiple spatial scales.</p>
                </div>

                <!-- Card 2: Real-Time Monitoring -->
                <div class="bg-gray-800 p-6 rounded-xl shadow-2xl border border-gray-700 transition duration-300 hover:border-teal-500 hover:shadow-teal-900/50">
                    <div class="text-teal-400 mb-4">
                        <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 10l4.553-2.276A1 1 0 0121 8.618v6.764a1 1 0 01-1.447.894L15 14M5 18h8a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v8a2 2 0 002 2z"></path></svg>
                    </div>
                    <h4 class="text-xl font-semibold mb-2 text-white">Live Real-Time Dashboard</h4>
                    <p class="text-gray-400">A modern web interface to visualize live streams, real-time vehicle counts, density heatmaps, and immediate congestion alerts.</p>
                </div>

                <!-- Card 3: Anomaly Detection -->
                <div class="bg-gray-800 p-6 rounded-xl shadow-2xl border border-gray-700 transition duration-300 hover:border-teal-500 hover:shadow-teal-900/50">
                    <div class="text-teal-400 mb-4">
                        <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                    </div>
                    <h4 class="text-xl font-semibold mb-2 text-white">Anomaly Identification</h4>
                    <p class="text-gray-400">Automated identification of traffic anomalies, incident detection, and unusual flow patterns for quick administrative response.</p>
                </div>
            </div>
        </section>

        <!-- Live Status/Dashboard Simulation Section -->
        <section id="dashboard" class="mb-20 bg-gray-900 p-8 rounded-xl border-l-4 border-teal-500">
            <h3 class="text-3xl font-bold mb-6 text-white flex items-center">
                <svg class="w-8 h-8 mr-3 text-red-500 animate-pulse" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM7 9a1 1 0 000 2h6a1 1 0 100-2H7z" clip-rule="evenodd"></path></svg>
                Real-Time Monitoring Status (Simulated)
            </h3>
            <div class="grid sm:grid-cols-3 gap-6">
                <!-- Status Card 1 -->
                <div class="bg-gray-800 p-4 rounded-lg">
                    <p class="text-sm font-medium text-gray-400">Active Cameras</p>
                    <p id="active-cameras" class="text-3xl font-bold text-teal-400 mt-1">--</p>
                </div>
                <!-- Status Card 2 -->
                <div class="bg-gray-800 p-4 rounded-lg">
                    <p class="text-sm font-medium text-gray-400">Vehicle Count (Last Min)</p>
                    <p id="vehicle-count" class="text-3xl font-bold text-white mt-1">--</p>
                </div>
                <!-- Status Card 3 -->
                <div class="bg-gray-800 p-4 rounded-lg">
                    <p class="text-sm font-medium text-gray-400">Congestion Level</p>
                    <p id="congestion-level" class="text-3xl font-bold text-yellow-500 mt-1">--</p>
                </div>
            </div>
            <p id="status-message" class="mt-4 text-sm text-gray-500 italic">Data refreshing in 5 seconds...</p>
        </section>

        <!-- Project Overview Section -->
        <section class="mb-20">
            <h3 class="text-3xl font-bold text-center mb-12 text-white">Project Vision & Architecture</h3>
            <div class="grid md:grid-cols-2 gap-12 items-center">
                <div class="space-y-6">
                    <h4 class="text-xl font-semibold text-teal-400">Modular & Scalable Design</h4>
                    <p class="text-gray-400">Our architecture is built in distinct layers—Data Input, Processing (CNN), API/WebSocket, and Presentation (Frontend). This ensures easy integration of future modules, such as license plate recognition, without overhauling the core system.</p>
                    <h4 class="text-xl font-semibold text-teal-400">Data-Driven Insights</h4>
                    <p class="text-gray-400">The system provides actionable insights to city administrators, moving beyond raw footage to deliver trend visualizations, lane occupancy statistics, and congestion patterns.</p>
                </div>
                <div class="p-6 bg-gray-800 rounded-xl shadow-xl">
                    <h4 class="text-lg font-bold mb-3 text-white">Frontend Technologies Used</h4>
                    <ul class="list-disc list-inside space-y-2 text-gray-300">
                        <li>Modern React + TypeScript Web Interface</li>
                        <li>Tailwind CSS for responsive, utility-first styling</li>
                        <li>Shadcn/ui Components for modular UI elements</li>
                        <li>Real-time WebSocket connection handling</li>
                    </ul>
                </div>
            </div>
        </section>

    </main>

    <!-- Footer -->
    <footer id="contact" class="bg-gray-900 border-t border-gray-700 py-8">
        <div class="max-w-7xl mx-auto container-padding text-center">
            <p class="text-lg font-semibold text-teal-400 mb-2">Adaptive Multi-Scale CNN Project</p>
            <p class="text-sm text-gray-500">&copy; 2025 Capstone Project. Lovely Professional University.</p>
            <p class="text-sm text-gray-500 mt-4">Contact: <a href="mailto:chinta.akhilesh@example.com" class="hover:underline text-teal-500">chinta.akhilesh@example.com</a></p>
        </div>
    </footer>

    <!-- JavaScript for Interactivity -->
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const mobileMenuButton = document.getElementById('mobile-menu-button');
            const mobileMenu = document.getElementById('mobile-menu');

            // 1. Mobile Menu Toggle
            mobileMenuButton.addEventListener('click', () => {
                mobileMenu.classList.toggle('hidden');
            });

            // 2. Simulated Dashboard Update Logic
            const activeCamerasEl = document.getElementById('active-cameras');
            const vehicleCountEl = document.getElementById('vehicle-count');
            const congestionLevelEl = document.getElementById('congestion-level');
            const statusMessageEl = document.getElementById('status-message');

            const statusMessages = [
                'Data stream optimized for low latency.',
                'Model inference cycle completed.',
                'Checking for system anomalies.',
                'Connection heartbeat nominal.'
            ];

            const updateDashboard = () => {
                // Generate simulated data
                const cameras = Math.floor(Math.random() * 5) + 8; // 8 to 12
                const vehicles = Math.floor(Math.random() * 500) + 1200; // 1200 to 1700

                let congestion = 'Low';
                let congestionColor = 'text-green-500';

                const randomCongestion = Math.random();
                if (randomCongestion > 0.85) {
                    congestion = 'High';
                    congestionColor = 'text-red-500';
                } else if (randomCongestion > 0.4) {
                    congestion = 'Moderate';
                    congestionColor = 'text-yellow-500';
                }

                // Update the DOM
                activeCamerasEl.textContent = cameras;
                vehicleCountEl.textContent = vehicles.toLocaleString();
                congestionLevelEl.textContent = congestion;

                // Reset congestion class and apply new one
                congestionLevelEl.className = `text-3xl font-bold mt-1 ${congestionColor}`;

                // Update status message
                const randomMsg = statusMessages[Math.floor(Math.random() * statusMessages.length)];
                statusMessageEl.textContent = `${randomMsg} Next update in 5 seconds...`;

                // Set timer for next update message
                let countdown = 5;
                const countdownInterval = setInterval(() => {
                    countdown--;
                    if (countdown > 0) {
                        statusMessageEl.textContent = `${randomMsg} Next update in ${countdown} seconds...`;
                    } else {
                        clearInterval(countdownInterval);
                    }
                }, 1000);
            };

            // Run the update every 5 seconds (5000 milliseconds)
            setInterval(updateDashboard, 5000);

            // Initial run
            updateDashboard();
        });
    </script>
</body>
</html>
